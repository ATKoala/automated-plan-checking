# format is like the following
# property_name = property_value
# property_name must be string
# property_value can only be string for now

default_input_folder = Resources/Input
default_output_folder = Reports
truth_table_file = Resources/truth_table_example.csv